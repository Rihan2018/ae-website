<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Astronaut Entertainment</title>
  <link rel="stylesheet" href="styles/style.css" />
</head>
<body>

<header class="hero-header">
  <img src="{{ url_for('protected_image', filename='logo.png') }}" alt="Astronaut Entertainment logo" class="logo-fixed" />

  <div class="hamburger-menu">
    <div class="hamburger-icon" id="hamburger-icon">&#9776;</div>
    <div class="menu-items" id="menu-items">
      <a href="home.html">Home</a>
      <a href="gallery.html">Gallery</a>
      <a href="about.html">About & Contact</a>
    </div>
  </div>
</header>

<div class="hero-carousel">
  <div class="carousel-track" id="hero-carousel-track">
    <img src="{{ url_for('protected_image', filename='banner1.jpg') }}" alt="Banner 1" class="carousel-image" />
    <img src="{{ url_for('protected_image', filename='banner2.jpg') }}" alt="Banner 2" class="carousel-image" />
    <img src="{{ url_for('protected_image', filename='banner3.jpg') }}" alt="Banner 3" class="carousel-image" />
  </div>
  <div class="banner-text">ASTRONAUT ENTERTAINMENT<br>North America Concert Promoter</div> 
  <button class="carousel-arrow left" id="carousel-left">&#10094;</button>
  <button class="carousel-arrow right" id="carousel-right">&#10095;</button>
  <div class="carousel-dots" id="carousel-dots"></div>
</div>

<section class="upcoming-events">
  <div class="section-header">
    <h2>Upcoming Events</h2>
  </div>

  <div class="scrolling-images">
    <div class="event-card">
      <img src="{{ url_for('protected_image', filename='Upcoming Events/1.png') }}" alt="Upcoming 1" />
      <div class="event-info">
        <div class="event-text">
          <p><strong>Location:</strong> Kuala Lumpur</p>
          <p><strong>Date:</strong> June 10, 2025</p>
          <p><strong>Details:</strong> A Dog Love to Love" is Singapore’s wildest new dating-comedy show! Led by viral cupid Ergou, expect improv chaos, meme-worthy skits, and surprise twists in the Love Mailbox.</p>
        </div>
        <span class="ticket-unavailable">🎫 Ticket available soon</span>
      </div>
    </div>

    <div class="event-card">
      <img src="{{ url_for('protected_image', filename='Upcoming Events/2.png') }}" alt="Upcoming 2" />
      <div class="event-info">
        <div class="event-text">
          <p><strong>Location:</strong> Singapore</p>
          <p><strong>Date:</strong> June 14, 2025</p>
          <p><strong>Details:</strong> A Dog Love to Love" is Singapore’s wildest new dating-comedy show! Led by viral cupid Ergou, expect improv chaos, meme-worthy skits, and surprise twists in the Love Mailbox.</p>
        </div>
        <span class="ticket-unavailable">🎫 Ticket available soon</span>
      </div>
    </div>

    <div class="event-card">
      <img src="{{ url_for('protected_image', filename='Upcoming Events/3.png') }}" alt="Upcoming 3" />
      <div class="event-info">
        <div class="event-text">
          <p><strong>Location:</strong> Tokyo</p>
          <p><strong>Date:</strong> June 20, 2025</p>
          <p><strong>Details:</strong> A Dog Love to Love" is Singapore’s wildest new dating-comedy show! Led by viral cupid Ergou, expect improv chaos, meme-worthy skits, and surprise twists in the Love Mailbox.</p>
        </div>
        <span class="ticket-unavailable">🎫 Ticket available soon</span>
      </div>
    </div>
  </div>
</section>

<section class="past-events">
  <div class="section-header">
    <h2>Past Events</h2>
  </div>

  <div class="scrolling-past">
    <div class="pastevent-card">
      <img src="{{ url_for('protected_image', filename='Past Events/Past1.png') }}" alt="Past Event 1" />
    </div>

    <div class="pastevent-card">
      <img src="{{ url_for('protected_image', filename='Past Events/Past2.png') }}" alt="Past Event 2" />
    </div>

    <div class="pastevent-card">
      <img src="{{ url_for('protected_image', filename='Past Events/Past3.png') }}" alt="Past Event 3" />
    </div>

    <div class="pastevent-card">
      <img src="{{ url_for('protected_image', filename='Past Events/Past4.png') }}" alt="Past Event 4" />
    </div>   
  </div>
</section>

<footer>
  <p>&copy; 2023 Astronaut Entertainment. All rights reserved.</p>
</footer>

<!-- Existing JS (unchanged) -->
<script>
  const hamburgerIcon = document.getElementById('hamburger-icon');
  const menuItems = document.getElementById('menu-items');

  hamburgerIcon.addEventListener('click', () => {
    menuItems.style.display = menuItems.style.display === 'block' ? 'none' : 'block';
  });

  window.addEventListener('click', (e) => {
    if (!e.target.closest('.hamburger-menu')) {
      menuItems.style.display = 'none';
    }
  });
</script>
<script>
  const track = document.getElementById('hero-carousel-track');
  const left = document.getElementById('carousel-left');
  const right = document.getElementById('carousel-right');
  const dotsContainer = document.getElementById('carousel-dots');
  const slides = document.querySelectorAll('.carousel-image');
  let index = 0;

  slides.forEach((_, i) => {
    const dot = document.createElement('span');
    if (i === 0) dot.classList.add('active');
    dot.dataset.index = i;
    dotsContainer.appendChild(dot);
  });

  const dots = document.querySelectorAll('.carousel-dots span');

  function updateCarousel() {
    track.style.transform = `translateX(-${index * 100}%)`;
    dots.forEach(dot => dot.classList.remove('active'));
    dots[index].classList.add('active');
  }

  right.addEventListener('click', () => {
    index = (index + 1) % slides.length;
    updateCarousel();
  });

  left.addEventListener('click', () => {
    index = (index - 1 + slides.length) % slides.length;
    updateCarousel();
  });

  dots.forEach(dot => {
    dot.addEventListener('click', () => {
      index = parseInt(dot.dataset.index);
      updateCarousel();
    });
  });

  document.querySelectorAll('.event-card img').forEach(img => {
    img.addEventListener('click', () => {
      const alreadyZoomed = img.classList.contains('zoomed');
      document.querySelectorAll('.event-card img.zoomed').forEach(i => i.classList.remove('zoomed'));
      if (!alreadyZoomed) {
        img.classList.add('zoomed');
      }
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') img.classList.remove('zoomed');
    });
  });

  document.querySelectorAll('.pastevent-card img').forEach(img => {
    img.addEventListener('click', () => {
      const alreadyZoomed = img.classList.contains('zoomed');
      document.querySelectorAll('.pastevent-card img.zoomed').forEach(i => i.classList.remove('zoomed'));
      if (!alreadyZoomed) {
        img.classList.add('zoomed');
      }
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') img.classList.remove('zoomed');
    });
  });
</script>

</body>
</html>
